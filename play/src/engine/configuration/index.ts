import { optionCategories } from '../../../../shared/src/engine/configuration/optionCategories.js'
import { ui } from '../../../../shared/src/engine/configuration/ui.js'
import { options } from './options.js'

export const configuration = {
    optionCategories,
    options,
    ui,
}
